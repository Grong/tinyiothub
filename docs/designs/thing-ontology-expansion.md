---
status: ACTIVE
---
# CEO Plan: Thing Ontology — Post-Implementation Expansion Set
Generated by /plan-ceo-review on 2026-07-27
Branch: feature/thing-ontology | Mode: SELECTIVE EXPANSION
Repo: Grong/tinyiothub

## Context

The Thing Ontology mega-branch is fully implemented (144 commits, 817 tests green,
24 eng-review findings folded). D1 decided: **ship as-is, iterate in follow-up
branches** — merge is the next validation gate (R4: the ontology dies if not
exercised by real agent tasks soon).

## Vision

Every physical and logical entity in a deployment is a Thing with a model,
documents, and events; the agent operates the whole site through ontology tools;
the UI assembles itself from the ontology (A2UI); the platform gets measurably
smarter every iteration (eval harness).

## Scope Decisions (cherry-pick ceremony, all accepted)

| # | Proposal | Effort | Decision | Reasoning |
|---|----------|--------|----------|-----------|
| D3.1 | Real LLM for ontology summaries (replace StubLlmClient) | S-M | ACCEPTED | Only delivered feature that is currently fake — AI card shows placeholder text |
| D3.2 | Ontology-aware agent eval harness | M | ACCEPTED | R4 killer — measures whether the agent actually benefits (graph died unvalidated) |
| D3.3 | Events retention (occurrence-vs-status aware) | S | ACCEPTED | Cheap now; wrong-shaped purge gets riskier the longer it waits |
| D3.4 | E2 A2UI ontology-driven rendering | M-L | ACCEPTED | Highest platform potential; gated on canvas-renderer maturity check first |
| D3.5 | Template re-sync (explicit opt-in propagation) | M | ACCEPTED | Recovers marketplace update story without dual source of truth |
| D3.6 | search_knowledge FTS5 trigram | M | ACCEPTED | Agent's highest-frequency knowledge path; LIKE scan degrades at scale |

## Ship Order (revised per Outside Voice OV-5, usage-first)

```
merge mega-branch
  → D3.3 events retention        (S — must ALSO fix the two existing wrong-shaped
                                   purge fns: cleanup_old_events has no is_status
                                   exemption; clear_acknowledged_events deletes
                                   status rows. Zero callers today — fix or delete
                                   BEFORE any cron is registered)
  → D3.1 real LLM summaries      (S-M — output validated+sanitized before DB write;
                                   note: compute stays on read path by design,
                                   first view is multi-second — progress copy exists)
  → D3.4 E2 A2UI                 (M-L — BUILDER SPEC FIRST (OV-3): ontology→A2UI
                                   mapping (state/type→component), re-render triggers,
                                   confirm panel shape. The 'renderer maturity gate'
                                   was dropped — renderer is already live end-to-end)
  → D3.6 FTS5 trigram            (M — concrete trigger: p95 search_knowledge
                                   latency > 200ms OR docs > 500 per workspace)
  → D3.5 template re-sync        (M — INCLUDES version baseline (OV-4):
                                   template_version pinning or creation snapshot,
                                   else 3-way conflict detection is impossible)
  → D3.2 agent eval harness      (M — REFRAMED (OV-2): honest regression detection
                                   for the 9 tools, NOT validation. Spec: drives full
                                   zeroclaw agent loop vs real sqlx DB; fixed judge
                                   model + rubric; pass/fail thresholds; ≥20 tasks)
```

Each expansion ships as its own branch with its own review cycle.

## Outside Voice (Claude subagent, 2026-07-27) — 6 findings, all accepted

1. Existing purge functions are wrong-shaped for the mixed events table → D3.3 scope
2. Summary read-path latency (by design, noted); agent visibility claim refuted on
   verification (GetThingTool passes the full ThingResponse incl. ontology_summary)
3. Eval corpus is synthetic pre-launch → reframed as regression detection (OV-2)
4. A2UI maturity gate unfalsifiable (renderer live) → builder spec first (OV-3)
5. Re-sync needs version baseline for 3-way diff → added to D3.5 (OV-4)
6. Sequence optimized for lab not usage → usage-first order (OV-5)

## Key Implementation-Time Decisions (0E, pre-resolved)

- **D3.1:** provider config follows the agent subsystem's existing LLM client
  (zeroclaw/provider infra), NOT a new client; 10s timeout + stale degradation
  already built and tested in summary.rs.
- **D3.4:** gate = canvas renderer maturity check (agent/tools/canvas.rs +
  web A2UI renderer) BEFORE writing any builder code. If immature → stays in
  TODOS, no dead code this time (a2ui.rs lesson).
- **D3.3:** retention must exempt status rows (is_status=1) from time-based
  purge — the dedup row IS the live state of quiet devices.
- **D3.5:** re-sync is always explicit (per-thing button or bulk opt-in),
  never silent overwrite of user-modified instances.
- **D3.2:** eval corpus = ≥20 real ontology tasks across all 9 tools;
  scoring = tool-selection accuracy + answer quality (LLM-judged rubric).

## NOT in scope

- E3 legacy `commands`-key import compat (deleted per eng-review D10)
- thing_links semantic relations (YAGNI per design)
- Workspace knowledge graph (permanently retired)
