---
inclusion: always
---

- 不要写总结，除非用户指定
- 每次编译日志只获取最新的几条，避免使用过多的token分析，除非用户指定
- 构建脚本应抑制详细输出，只显示关键信息（开始、完成、错误），避免大量编译日志消耗token
- 部署脚本应抑制命令输出（使用 `2>&1 | Out-Null`），只显示操作步骤和结果
- 如需查看详细日志，用户可单独运行脚本或添加 --verbose 参数
- 不要一味的保证兼容性，现在系统没有正式发布，不需要考虑兼容性，只需要保留一种正确的方式即可