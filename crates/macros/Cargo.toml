[package]
name = "macros"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true
repository.workspace = true
description = "Procedural macros for TinyIoTHub"

[lib]
name = "tinyiothub_macros"
proc-macro = true

# 角色:proc macros（编译期代码生成）
# 禁止依赖:任何 workspace crate;规则出处:G 系列计划 Task 7 (G8)
[dependencies]
proc-macro2 = "1.0"
quote = "1.0"
syn = "1.0.90"
serde_json = "1.0"
serde_qs = "0.9"

[lints]
workspace = true
