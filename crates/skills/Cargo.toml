[package]
name = "skills"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true
repository.workspace = true
description = "Agent skills and tool registry — skill loading, tool trust engine, tool metadata"

[lib]
name = "tinyiothub_skills"

# 角色:agent 技能/工具注册表 + 信任引擎（纯逻辑）
# 禁止依赖:web、runtime、core 以外的领域 crate;规则出处:G 系列计划 Task 7 (G8)
[dependencies]
core = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
async-trait = { workspace = true }
thiserror = { workspace = true }

[dev-dependencies]
uuid = { workspace = true }

[lints]
workspace = true
