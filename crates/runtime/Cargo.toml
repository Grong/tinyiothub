[package]
name = "runtime"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true
repository.workspace = true
description = "Shared runtime infrastructure for TinyIoTHub (drivers, event bus, data server)"

[lib]
name = "tinyiothub_runtime"

[dependencies]
core = { workspace = true }
macros = { workspace = true }
plugin-sdk = { workspace = true, features = ["core-interop"] }
db = { workspace = true }

# Async runtime
tokio = { workspace = true, features = ["process", "sync"] }

# Serialization
serde = { workspace = true }
serde_json = { workspace = true }

# Async trait
async-trait = { workspace = true }

# Error handling
thiserror = { workspace = true }

# Logging
tracing = { workspace = true }

# Collections
dashmap = { workspace = true }

# Random
rand = { workspace = true }

# Serial (optional)
serialport = { workspace = true, optional = true }

# Caching
moka = { workspace = true }

# Sync primitives
parking_lot = { workspace = true }
arc-swap = "1.9"

# Futures utilities (join_all)
futures-util = { workspace = true }

# Dynamic library loading
libloading = { workspace = true }

# Date/time
chrono = { workspace = true }

# Regex
regex = { workspace = true }

[dev-dependencies]
tokio = { workspace = true }
