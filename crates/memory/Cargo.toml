[package]
name = "memory"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true
repository.workspace = true
description = "Agent memory persistence — SqliteAgentMemoryRepository + embedding layer"

[lib]
name = "tinyiothub_memory"

# 角色:agent 记忆持久化引擎（MemoryService）——纯逻辑已迁入 crates/agent（Task 12, D10'）
# 禁止依赖:apps/*、web、runtime —— 由组合层调用;规则出处:G 系列计划 Task 7 (G8)
[dependencies]
core = { workspace = true }
agent = { workspace = true }
db = { path = "../db" }
llm = { path = "../llm" }
serde_json = { workspace = true }
sqlx = { workspace = true }
async-trait = { workspace = true }
anyhow = { workspace = true }
tokio = { workspace = true }
tracing = { workspace = true }
chrono = { workspace = true }
dashmap = { workspace = true }

[dev-dependencies]
tokio = { workspace = true, features = ["test-util"] }

[lints]
workspace = true
