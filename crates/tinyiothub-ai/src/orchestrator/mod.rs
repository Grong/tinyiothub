//! AI subsystem orchestrator — top-level coordinator.
//! Will be populated in a later task.

/// Orchestrator handle for the AI subsystem.
pub struct Orchestrator;

impl Orchestrator {
    pub async fn shutdown(&self) {}
}
