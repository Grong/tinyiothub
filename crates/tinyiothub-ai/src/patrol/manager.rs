//! Patrol manager — orchestrates agent patrol loops.
//! Will be populated in Task 4.

/// Patrol manager handle.
pub struct PatrolManager;

impl PatrolManager {
    pub async fn shutdown(&self) {}
}
